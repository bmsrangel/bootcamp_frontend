@import "./normalize"

*, *::before, *::after
    box-sizing: border-box

a, input, button
    transition: .25s all ease-in-out

body
    font-size: 16px
    width: 100vw
    height: 100vh
    overflow-x: hidden //omite a barra de rolagem horizontal

@import "./_stylus/_grid"
